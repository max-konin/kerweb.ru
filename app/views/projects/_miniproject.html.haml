%li
	%a{"data-description" => project.body, "data-largesrc" => project.picture.url(:fit), "data-title" => project.name, :href => project.href}
		%img{ :src => project.pictureMini.url(:mini), :alt => project.name }
