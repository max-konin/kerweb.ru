- content_for :title do
  =@post.title
- content_for :description do
  =@post.preview

%h1
  =@post.title
.post_preview
  =@post.preview unless @post.preview.blank?
.post_body
  =@post.body unless @post.preview.blank?


